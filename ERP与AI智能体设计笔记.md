# ERP 与 AI 智能体设计笔记

> 整理自手写笔记（共 4 页）。个别字迹潦草处按最接近的内容辨识，如与原意有出入请以笔记原文为准。

## 一、智能体（Agent）的基本构成

智能体由四大要素组成：

| 要素 | 对应能力 | 具体说明 |
|------|---------|---------|
| 大脑 | LLM | 大语言模型，负责思考与决策 |
| 记忆 | 短期 + 长期 | 短期记忆对应上下文；长期记忆对应向量数据库 |
| 眼睛 | 信息输入 | 接收外部信息：网页、文档等 |
| 手脚 | 行动执行 | 执行实际操作：调用函数或 API、执行 SQL、操作 ERP |

### 智能体的两种驱动模式

1. **工作流驱动**（代表：Dify、Coze）：适用于确定性业务流程，大模型仅做辅助（语义理解、文本生成）。
2. **目标驱动**：给 AI 提供一组 API 和一套规则，给它下达目标，让它自主完成。缺点：不可控，容易死循环。

### 智能体平台的整体形态

用户 → 入口 → 智能体平台，平台内部由多个专职 Agent 组成：对话 Agent、检查 Agent、阅读 Agent 等。

## 二、ERP 系统：MVP 阶段规划

按模块由浅入深建设，优先级从高到低：

| 模块 | 定位 | 功能范围 |
|------|------|---------|
| OA | 基础 | 组织架构、员工、角色权限、审批流 |
| CRM（营销/销售） | 基础 | 客户线索、跟进记录、报价单、合同管理 |
| 进销存 | 核心 | 采购单、供应商管理、库存盘点、出入库/发货单 |
| 财务 | 轻量 | 应收应付、费用报销、基础收支流水 |
| 生产/项目管理 | 可选 | BOM 表、任务进度追踪、工时记录 |

## 三、技术选型

- **后端**：Spring Boot（Java）/ NestJS（Node.js）/ Go（Gin）
- **前端**：Vue3 + Element Plus，或 React + Ant Design
- **数据库**：PostgreSQL（关系核心）+ Redis（缓存/队列）
- **架构设计原则**：API 优先——所有业务逻辑必须通过 Service 层提供 RESTful 或 GraphQL API

## 四、智能体平台设计

### 1. 分层架构设计

独立的服务部署，通过 API 与 ERP 进行交互，自上而下分为三层：

**（1）感知层（输入层）**

- 对话界面：Web 端嵌入 ERP、钉钉/企微机器人
- 事件触发器：如 ERP 中库存低于阈值时，自动唤醒库存 Agent

**（2）认知与决策层（大脑——Agent Core）**

- 大模型路由：简单任务用小模型，复杂任务用大模型
- 记忆系统：短期记忆（对话上下文）+ 长期记忆（向量化存入 Milvus / PgVector，如历史操作规律、公司规章制度）
- 规划能力：ReAct（推理 + 行动）模式，将复杂指令拆解为多步 ERP 操作

**（3）行动层（输出端——Tools/MCP）**

- 将 ERP 的 API 封装为标准工具，供 Agent 调用

### 2. 典型业务智能体场景

**（1）销售跟单 Agent**

- 能力：自动分析 CRM 未成交客户，结合历史成交数据生成跟进话术；自动读微信/企微聊天记录，提取结构化信息存入 CRM
- 工具调用：查询客户历史 API、更新跟进记录 API、生成报价单 API

**（2）库存管家 Agent**

- 能力：监控库存水位，预测未来一周缺货风险，自动草拟采购单发给供应链
- 工具调用：查询库存 API、查询供应商 API、创建采购单 API

**（3）财务对账 Agent**

- 能力：每日自动拉取收支流水，与银行流水 RAG 比对，标记异常账单，写催款邮件草稿
- 工具调用：查询账单 API、发送邮件工具、创建催款任务 API

**（4）企业数据问答 Agent**

- 能力：老板输入自然语言问题，Agent 自动转 SQL/API 查询，生成图表和结论

## 五、ERP 与 Agent 协同

### 1. 采用 MCP 标准对接

在 ERP 和 Agent 之间建立一个 MCP Server：

> ERP 暴露 API → MCP Server 将其包装为 AI 可理解的工具 → Agent 通过 MCP 协议调用

好处：ERP 开发人员不需要懂 AI prompt，只需懂 ERP 内部复杂的表结构。

### 2. 引入"人机协同审批流"（Human in the Loop 机制）

流程示例：

> Agent 生成动作（如创建了一张 10 万采购单）→ 推送消息给销售经理 → 经理在 ERP 中点击"同意" → Agent 执行最终确认 API

关键要求：将 Agent 的动作记录与 ERP 的审批状态完全打通，做到所有 AI 操作可追溯。

### 3. 数据治理与 RAG 准备

在 ERP 数据库设计时，增加"非结构化数据"的存储字段（如 JSON），用于存储销售沟通记录、客户反馈等。这些数据后续会通过 ETL 清洗后，灌入智能体平台的向量数据库，作为 Agent 的长期记忆。

## 六、落地建议与演进路线图（一年为期）

| 阶段 | 目标 | 关键动作 |
|------|------|---------|
| 1–3 月 | ERP MVP | 跑通进销存 + 财务核心链路，强制要求所有操作走 API |
| 4–6 月 | 数据流梳理与接口封装 | 完善 CRM 和 OA，将所有业务 API 封装化，部署 MCP Server |
| 7–9 月 | 单点 Agent 突破 | 先不上平台，先做企业数据问答 Agent 和库存预警 Agent 两个高频痛点场景，跑通"大模型 + ERP API"的闭环 |
| 10–12 月 | 智能体平台化 | 引入多 Agent 编排（LangGraph 或 Dify），让各个 Agent 协同工作 |

多 Agent 协同示例：销售 Agent 接单 → 触发库存 Agent 检查 → 发现缺货自动由采购 Agent 下单。

## 七、总结

一句话概括整体思路：**ERP 骨架、API 神经、Agent 大脑。**
